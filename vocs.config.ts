import { defineConfig } from 'vocs'

export default defineConfig({
  description: 'Build reliable apps & libraries with lightweight, composable, and type-safe modules that interface with smart accounts',
  title: 'Docs',
  rootDir: './docs',
  sidebar: [
    {
      text: 'Getting Started',
      link: '/getting-started',
    },
  ],
})
